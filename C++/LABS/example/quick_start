Быстрый старт
--------------
1) Требования: любой C++17-компилятор (g++/clang++/MSVC). В консоли должно работать очистка экрана (`cls` в коде; на *nix можно заменить на `clear` или просто игнорировать сообщение об ошибке).
2) Сборка из корня проекта (пример для g++/clang++):
   g++ -std=c++17 -O2 -Wall -Wextra -pedantic main.cpp Airplane.cpp Auto.cpp Train.cpp CargoCrarrier.cpp File.cpp com_fun.cpp -o lab7
3) Запуск:
   ./lab7
4) В приложении сначала выберите класс транспорта, затем контейнер (OneWayQueue или STL multimap), а дальше работайте через меню (вывод, добавление, удаление, сортировка, поиск, чтение/запись файлов).
5) Windows-альтернатива: откройте `Lab 7.sln` в Visual Studio, соберите конфигурацию Debug/Release и запустите получившийся исполняемый файл из каталога `x64/<Config>/`.
